# 9/2 세 번째 업데이트(보안 강화): pip-audit로 스캔한 결과 python-dotenv/pillow(전이
# 의존성)/starlette(전이 의존성, fastapi가 끌고 옴)에서 알려진 취약점 35건이 나와서
# 전부 패치된 버전으로 올렸습니다. fastapi==0.121.0 dry-run 확인 결과 pydantic 2.9.2와
# 그대로 호환되어(핀 안 올려도 됨) 0.141.1까지 갈 필요는 없었지만, starlette가 0.49.3까지만
# 따라오고 일부 취약점(PYSEC-2026-248/249/2280/2281)은 starlette>=1.1.0~1.3.1을 요구해서
# fastapi도 0.141.1로, starlette도 1.6.0으로 직접 명시했습니다(둘 다 fastapi의
# `starlette>=0.46.0` 제약과 충돌 없음). google-genai는 0.3.0에서 pillow<12.0.0을
# 강제해서 pillow==12.3.0 핀과 충돌했는데, google-genai>=1.0.0부터는 pillow가 아예
# 필수 의존성에서 빠져서(선택 extra로만 남음) 1.28.0으로 올려 해결했습니다.
# 프리 DB로부터의 전체 자동 시딩 + 전 API 엔드포인트(gig-score/income/budgets/connect
# 플로우 포함)를 이 조합으로 재검증 완료. pip-audit 재실행 결과 이 5개 패키지 관련
# 취약점은 0건(남은 건 pip/setuptools 자체의 빌드 툴체인 취약점으로, requirements.txt에
# 없고 배포되는 앱 코드와 무관).
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
redis==5.0.8
pydantic==2.9.2
python-dotenv==1.2.3
scikit-learn==1.8.0
pandas==2.2.3
numpy==1.26.4
google-genai==1.28.0
pillow==12.3.0
